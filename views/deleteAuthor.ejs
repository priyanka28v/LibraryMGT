<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DELETE AUTHOR</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: url('https://images.unsplash.com/photo-1521587760476-6c12a4b040da?auto=format&fit=crop&w=1740&q=80') no-repeat center center fixed;
            background-size: cover;
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #fff;
            position: relative;
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            /* Darkened overlay for contrast */
            z-index: 0;
        }

        form {
            backdrop-filter: blur(10px);
            /* background-color: rgba(0, 0, 0, 0.7); */
            border: 1px solid rgba(255, 255, 255, 0.3);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.6);
            width: 380px;
            z-index: 1;
            text-align: center;
        }

        h1 {
            margin-bottom: 20px;
            font-size: 28px;
            color: white;
            /* A warm color for the title */
        }

        label {
            font-weight: bold;
            display: block;
            margin-top: 12px;
            color: #f0f0f0;
        }

        input[type="text"] {
            width: 100%;
            padding: 12px;
            margin-top: 8px;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            background-color: rgba(255, 255, 255, 0.9);
            color: #333;
        }

        button {
            color: white;
            border: none;
            padding: 12px 20px;
            margin-top: 20px;
            font-size: 16px;
            border-radius: 8px;
            cursor: pointer;
            transition: 0.3s ease-in-out;
            width: 100%;
        }

        button[type="submit"] {
            background-color: #e74c3c;
        }

        button[type="submit"]:hover {
            background-color: #c0392b;
        }

        .back-button {
            background-color: #198754;
            margin-top: 10px;
        }

        .back-button:hover {
            background-color: #146c43;
        }

        p {
            color: #ff6b6b;
            margin-top: 15px;
            font-weight: bold;
        }
    </style>
</head>

<body>
    <form action="/authors/deleteAuthor" method="post">

        <fieldset>
            <label for="AuthorId">ENTER AUTHOR ID:</label>
            <input type="text" placeholder="ENTER AUTHOR ID" id="AuthorId" name="AuthorId" required>

            <button type="submit">DELETE</button>
            <button class="back-button" type="button" onclick="window.history.back();">Back</button>

            <% if (message) { %>
                <p>
                    <%= message %>
                </p>
                <% } %>
        </fieldset>
    </form>
</body>

</html>