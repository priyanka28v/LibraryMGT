<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BOOK ID</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background: url('https://images.unsplash.com/photo-1521587760476-6c12a4b040da?auto=format&fit=crop&w=1740&q=80') no-repeat center center fixed;
            background-size: cover;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            position: relative;
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 0;
        }

        /* Background layer with blur */
        .background-blur {
            position: fixed;
            top: 0;
            left: 0;
            height: 100%;
            width: 100%;
            background: url('https://images.unsplash.com/photo-1521587760476-6c12a4b040da?auto=format&fit=crop&w=1740&q=80') no-repeat center center/cover;

            z-index: -1;
        }

        form {
            backdrop-filter: blur(12px);
            background-color: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.3);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
            width: 350px;
            z-index: 1;
            color: white;
        }

        h1 {
            font-size: 20px;
            margin-bottom: 15px;
            font-weight: bold;
        }

        label {
            font-weight: bold;
            display: block;
            margin-bottom: 5px;
        }

        input {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            margin-bottom: 10px;
        }

        button:hover {
            background-color: #0056b3;
        }

        .back-button {
            background-color: #28a745;
            color: white;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            padding: 10px;
            width: 100%;
        }

        .back-button:hover {
            background-color: #218838;
        }

        /* Error Message Styling */
        p {
            text-align: center;
            color: red;
            font-size: 16px;
            font-weight: bold;
        }
    </style>
</head>

<body>

    <div class="form-container">
        <form action="/books/updateBookId" method="post">
            <h1>ENTER BOOK ID</h1>
            <label for="bookId">Book ID:</label>
            <input type="text" id="bookId" name="BookId" placeholder="Enter Book ID" required>
            <button type="submit">Submit</button>
            <button class="button back-button" type="button" onclick="back()">Back</button>
            <% if (message) { %>
                <p class="error-message">
                    <%= message %>
                </p>
                <% } %>
        </form>
    </div>

</body>
<script>
    function back() {
        window.location.href = '/Books'
    }
</script>

</html>