<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library Home</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background: url("https://www.pixel-studios.com/blog/wp-content/uploads/2018/12/012-1200x600.jpg") no-repeat center center/cover;
            height: 100vh;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            height: 100%;
            width: 100%;
            background: rgba(0, 0, 0, 0.6);
            z-index: 1;
        }

        h1 {
            z-index: 2;
            font-size: 36px;
            font-weight: 700;
            color: #fff;
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.8);
            margin-bottom: 20px;
        }

        .container {
            position: relative;
            z-index: 2;
            border-radius: 20px;
            padding: 40px 30px;
            width: 90%;
            max-width: 450px;
            text-align: center;
            color: #fff;
        }

        .button {
            background: linear-gradient(135deg, #00c6ff, #0072ff);
            color: #fff;
            padding: 14px 30px;
            margin: 12px;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 500;
            box-shadow: 0 6px 20px rgba(0, 123, 255, 0.3);
            transition: all 0.3s ease;
        }

        .button:hover {
            transform: scale(1.08);
            background: linear-gradient(135deg, #0072ff, #00c6ff);
            box-shadow: 0 8px 25px rgba(0, 123, 255, 0.5);
        }

        @media (max-width: 480px) {
            h1 {
                font-size: 26px;
                text-align: center;
                padding: 0 10px;
            }

            .container {
                padding: 30px 20px;
            }

            .button {
                width: 100%;
                margin: 10px 0;
            }
        }
    </style>
</head>

<body>

    <div class="overlay"></div>

    <h1>Welcome to the Library Management System</h1>

    <div class="container">
        <nav>
            <button class="button" onclick="login()">Login</button>
            <button class="button" onclick="signup()">Signup</button>
        </nav>
    </div>

    <script>
        function login() {
            window.location.href = "/login";
        }

        function signup() {
            window.location.href = "/signup";
        }
    </script>

</body>

</html>