<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UPDATE AUTHOR DETAILS</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: url('https://images.unsplash.com/photo-1521587760476-6c12a4b040da?auto=format&fit=crop&w=1740&q=80') no-repeat center center fixed;
            background-size: cover;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            position: relative;
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 0;
        }

        /* Background layer with blur */
        .background-blur {
            position: fixed;
            top: 0;
            left: 0;
            height: 100%;
            width: 100%;
            background: url('https://images.unsplash.com/photo-1521587760476-6c12a4b040da?auto=format&fit=crop&w=1740&q=80') no-repeat center center/cover;

            z-index: -1;
        }

        form {
            backdrop-filter: blur(12px);
            background-color: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.3);
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
            width: 350px;
            z-index: 1;
            color: white;
        }

        h1 {
            margin-bottom: 20px;
            font-size: 20px;
            color: white;
            /* A warm color for the title */
        }

        label {
            font-weight: bold;
            display: block;
            margin-top: 12px;
            color: #f0f0f0;
        }

        input[type="text"] {
            width: 100%;
            padding: 12px;
            margin-top: 8px;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            background-color: rgba(255, 255, 255, 0.9);
            color: #333;
        }

        button {
            color: white;
            border: none;
            padding: 12px 20px;
            margin-top: 20px;
            font-size: 16px;
            border-radius: 8px;
            cursor: pointer;
            transition: 0.3s ease-in-out;
            width: 100%;
        }

        button[type="submit"] {
            background-color: #0d6efd;
        }

        button[type="submit"]:hover {
            background-color: #084298;
        }

        p {
            color: #ff6b6b;
            margin-top: 15px;
            font-weight: bold;
        }

        .back-button {
            background-color: #28a745;
            color: white;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            padding: 10px;
            width: 100%;
        }

        .back-button:hover {
            background-color: #218838;
        }
    </style>
</head>

<body>
    <form action="/authors/updateAuthor" method="post">
        <h1>UPDATE AUTHOR DETAILS:</h1>
        <fieldset>
            <input type="text" name="AuthorId" value="<%= author._id %>" readonly>

            <label for="name">NAME:</label>
            <input type="text" placeholder="ENTER NAME" id="name" name="name" value="<%= author.name %>" required>

            <label for="bio">ENTER BIO:</label>
            <input type="text" placeholder="ENTER BIO" id="bio" name="bio" value="<%= author.bio %>" required>

            <label for="nationality">ENTER NATIONALITY:</label>
            <input type="text" placeholder="ENTER NATIONALITY" id="nationality" name="nationality"
                value="<%= author.nationality %>" required>

            <button type="submit">UPDATE</button>
            <button class="button back-button" type="button" onclick="window.history.back();">Back</button>
        </fieldset>
    </form>
    </div>
</body>

</html>